This directory (model_demo_LJ_MI_OPBC_H_C) contains a Lennard Jones (LJ) Model
with a cutoff radius written in C.  It complies with the KIM API interface
standard and uses a cutoff radius and a neighbor list supplied by a Test.

The following files are in this directory:

Makefile
     Makefile to compile and build executables with GNU or Intel compilers.

README
     This file.

model_demo_P_LJ_MI_OPBC_H_C.c
     C module file that contains the implementation of the LJ potential.

model_demo_P_LJ_MI_OPBC_H_C.kim
     KIM descriptor file, contains all variables and methods that are exposed
     through the KIM API interface.
